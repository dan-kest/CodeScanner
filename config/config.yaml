app:
  port: 3000
  paging:
    maxItemPerPage: 50
  scan:
    localRepoPath: /tmp/cscanner/git-repo/
    workerCount: 5
    ignore: .git,.vscode,.idea # Comma delimited

postgres:
  connectionTimeout: 60000
  isPrintLog: true

rabbitmq:
  publishTimeout: 5000
  queue:
    name: scanner
